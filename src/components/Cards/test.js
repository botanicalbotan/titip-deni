const fruits = ["apple", "orange", "cherry"];

const blue = ["apple", "orange", "cherry"];


{fruits.map((post) => (
    <tr> 
    <td>{post}</td>
    </tr> )
    
    (blue.map((pos) => (
    <tr> 
    <td>{pos}</td>
    </tr>
    )
    )))}